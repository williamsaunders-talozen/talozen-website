{
  "name": "talozen-website",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "npm run build && npx wrangler deploy"
  },
  "dependencies": {
    "@astrojs/mdx": "^5.0.3",
    "astro": "^6.1.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260408.1"
  }
}
